// retorna o tamanho de uma string
const text = 'texto'.length; 

// Retorna um array quebrando a string por um delimitador
const text = 'texto'.split('x');

//busca por um valor e substitui por outro
const text = 'texto'.replace('text', 'TEXT'); 

//retorna a fatia de um valor
const text = 'texto'.slice(-1); //ultimo caractere

const text = 'texto'.slice(0, -1);//da primeira menos a ultima letra

const text = 'texto'.slice(1);//da segunda até a última letra